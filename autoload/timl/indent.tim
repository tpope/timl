(defmacro include-guard [...]
  (let ((param (or (first ...) 'b:did-indent)))
   `(timl#core#include-guard ,param)))

(defmacro setlocal [...]
  `(begin
     (: (string "setlocal " ,(build-option-string ...)))
     (set! b:undo-indent ,(extract-option-restore ...))))
