(in-ns 'timl#math)

(define (fact x)
  (loop ((n x)
         (f 1))
    (if (<= n 1) f (recur (1- n) (* f n)))))

(define (fib n)
  (first
    (loop ((xs (list 1 0)))
      (if (< (count xs) n)
        (recur (cons (+ (first xs) (second xs)) xs))
        xs))))
