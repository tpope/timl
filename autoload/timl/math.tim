(ns timl#math)

(defn fact [x]
  (loop [n x
         f 1]
    (if (<= n 1) f (recur (dec n) (* f n)))))

(defn fib [n]
  (first
    (loop [xs [1 0]]
      (if (< (count xs) n)
        (recur (cons (+ (first xs) (second xs)) xs))
        xs))))
