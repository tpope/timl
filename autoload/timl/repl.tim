(defun source (fn)
  (if-let (impl (get timl#lambdas (string fn) nil))
          (list 'lambda
                (get impl "arglist")
                (get impl "form"))))

(defun doc (fn)
  (:help (string fn "("))
  (:throw (f:string "timl#repl: EXIT")))
